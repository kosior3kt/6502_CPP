#ifndef __DEFINES__
#define __DEFINES__

using Byte = unsigned char;
using Word = unsigned short;
using u32 = unsigned int;
using s32 = int;

#define C_f  (1 << 0)
#define Z_f  (1 << 1)
#define I_f  (1 << 2)
#define D_f  (1 << 3)
#define B_f  (1 << 4)
#define V_f  (1 << 5)
#define N_f  (1 << 6)

#endif
